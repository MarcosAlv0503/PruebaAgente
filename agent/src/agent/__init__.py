"""Loang IA agent backend package.

Skeleton for new projects cloned from ``loang-template-agent``. Each project
replaces the documented placeholders with its own domain logic. Run
``make verify-template-cleanup`` to list everything still pending.
"""

from __future__ import annotations

__version__ = "0.1.1"
