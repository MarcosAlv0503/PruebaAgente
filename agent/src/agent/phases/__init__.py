"""Per-phase logic of the agent.

Convention: one file per phase, named ``NN_<short_name>.py`` where ``NN`` is
the order in which the phase runs. The graph in ``agent.graph`` imports them
explicitly. v0.1.0 ships with the placeholder ``_example.py``.
"""
