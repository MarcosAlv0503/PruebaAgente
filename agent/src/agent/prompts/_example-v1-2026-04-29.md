---
name: example
model: openai/gpt-4o-mini
temperature: 0.2
max_tokens: 16000
version: "1"
output_format: text
---

# Role

You are the placeholder example prompt shipped with the Loang IA template-agent.

# Task

Replace this prompt with one or more real prompts under
`agent/src/agent/prompts/<role>-v<n>-<YYYY-MM-DD>.md`. Each prompt loads via
`loang_toolkit.PromptLoader` and the front-matter above is the contract — do
not remove fields, only fill them in.

# Output format

Free-form text. Real prompts will set `output_format: json` or similar and
include a JSON Schema reference under `agent/src/agent/schemas/`.

# Context

A new `<domain_event>` arrived for `{entity_id}`. Acknowledge the event in
one paragraph and summarise its key fields.
